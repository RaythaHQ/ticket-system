@page "/admin/tickets/teams/create"
@model App.Web.Areas.Admin.Pages.Teams.Create

@{
    var pageTitle = "Create team";
    Layout = "SidebarLayout";
    ViewData["Title"] = pageTitle;
    ViewData["ActiveMenu"] = "Teams";
    ViewData["ExpandTicketingMenu"] = true;

    BackLinkOptions back = new BackLinkOptions
    {
        Page = RouteNames.Teams.Index
    };
}

<div>
    <div class="row mb-4">
        <partial name="_Partials/PageHeading" model='ViewData["Title"]' />
        <div class="col-xxl-7 col-xl-8 col-lg-9 col-md-12">
            <div class="card border-0 shadow mb-4">
                <div class="card-body">
                    @(await Html.PartialAsync("_Partials/BackToList", back))
                    <form asp-page="@RouteNames.Teams.Create" method="post" class="py-4" novalidate>
                        <div class="col-lg-12">
                            <div class="mb-3">
                                <label class="form-label raytha-required" asp-for="Form.Name"></label>
                                <input type="text" class="form-control @Model.HasError("Name")" asp-for="Form.Name"
                                    required>
                                <div class="invalid-feedback">@Model.ErrorMessageFor("Name")</div>
                            </div>
                        </div>

                        <div class="col-lg-12">
                            <div class="mb-3">
                                <label class="form-label" asp-for="Form.Description"></label>
                                <textarea class="form-control @Model.HasError("Description")" asp-for="Form.Description"
                                    rows="3"></textarea>
                                <div class="invalid-feedback">@Model.ErrorMessageFor("Description")</div>
                            </div>
                        </div>

                        <div class="col-lg-12">
                            <div class="form-check">
                                <input class="form-check-input @Model.HasError("RoundRobinEnabled")" type="checkbox"
                                    asp-for="Form.RoundRobinEnabled">
                                <label class="form-check-label" asp-for="Form.RoundRobinEnabled">
                                    Enable Round Robin Assignment
                                </label>
                                <div class="invalid-feedback">@Model.ErrorMessageFor("RoundRobinEnabled")</div>
                                <div class="form-text">When enabled, new unassigned tickets for this team will be
                                    automatically assigned to eligible members.</div>
                            </div>
                        </div>

                        <button type="submit" class="btn btn-success mt-4">Save changes</button>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
