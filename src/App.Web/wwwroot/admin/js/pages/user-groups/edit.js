/**
 * User Groups - Edit Page
 * Handles user group editing form
 */

import { ready } from '/admin/js/core/events.js';

function init() {
  // No special logic needed for edit page currently
  // Developer name is not editable after creation
}

ready(init);

